//create seeds for example clients as well as example blog posts.

// const db = require('./connection');

// const { Client, Post } = require('../models');

// db.once("open", async () => {
//     await Client.deleteMany();

//     const clients = await Client.insertMany([
//         {name:}
//     ])
// })